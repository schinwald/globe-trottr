syntax = "proto3";

package core.v1.messages;

message RoomConnections {
  repeated User users = 1;
}

message User {
  string id = 1;
  string username = 2;
  Role role = 3;
}

enum Role {
  ROLE_UNSPECIFIED = 0;
  ROLE_HOST = 1;
  ROLE_GUEST = 2;
}
